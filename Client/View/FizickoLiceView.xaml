<UserControl x:Class="Client.View.FizickoLiceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" Width="361">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Row="1" Margin="5" 
                  AutoGenerateColumns="False"
				  ItemsSource="{Binding ListOfFizickaLica}"
                  SelectedItem="{Binding Path=SelectedFizickoLice, Mode=TwoWay}"
				  >
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Ime}"
									Header="Ime"/>
                <DataGridTextColumn Binding="{Binding Path=Prezime}"
									Header="Prezime"/>
                <DataGridTextColumn Binding="{Binding Path=JMBG}"
									Header="JMBG"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" Width="180">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Ime: " />
            <TextBox Text="{Binding Path=FizickoLice.Ime, Mode=TwoWay}" Grid.Column="1" />

            <TextBlock Text="Prezime: " Grid.Row="1" />
            <TextBox Text="{Binding Path=FizickoLice.Prezime, Mode=TwoWay}" Grid.Column="1" Grid.Row="1" />


        </Grid>
    </Grid>
</UserControl>
