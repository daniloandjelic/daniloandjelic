<UserControl x:Class="Client.View.NewUpdateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TabControl DisplayMemberPath="Name" SelectedIndex="0"
            ItemsSource="{Binding PageViewModels}">
            <!--<TabControl.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding CurrentPageViewModel.Name}"/>
                </DataTemplate>
            </TabControl.ItemTemplate>-->

            <!--<TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter Property="Header" Value="{Binding CurrentPageViewModel.Name}"/>
                </Style>
            </TabControl.ItemContainerStyle>-->

        </TabControl>

    </Grid>
</UserControl>
